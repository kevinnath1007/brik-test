import {DeviceType} from 'react-native-device-info';

export const Monitoring = {
    logError: (event: string, params: any) => {
        // blm sempet setup tadinya mau firebase crashlytics & sentry bagusnya
        // sentry bisa dapet specific line JS kalo error
        // crashlytics buat crash native

    }
}
